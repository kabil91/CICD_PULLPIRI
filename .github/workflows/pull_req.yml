name: CI On Pull Request

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - '**/*.rs'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'scripts/**'
jobs:
  run-core-ci:
    uses: ./.github/workflows/run-ci.yml
